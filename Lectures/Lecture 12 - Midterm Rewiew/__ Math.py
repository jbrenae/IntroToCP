                    
                    
                    #----- Syntax -----#

#    a+b    (Addition)
#    a-b    (Subtraction)
#    a*b    (multiplication)
#    a/b    (Division)
#    a//b   (FLOOR division) -- eg. 5/2=2 -- Two goes into 5 only twice. 
#    a%b    a mod b (Modulo) -- eg. 5%2=1 -- Two goes into 5 only twice with a remainder of 1.

                    #----- Integers -----#

    # Can be easily counted i.e, 1,2,3... and 0, -1, -2, -3
    # Float - nuimbers iwth decimals since it can "float" around a number

                #----- Python type conversions -----#

    # type() - Determins the type of number
    # int() - Drops the decimals
    # round() - Rounds up or down
    # float() - Changes to a decimals
    # str() - Changes to a string

                    # ----- Incrementing -----#

# Incrementing: Sum = sum + 1
# Decrementing: Sum = sum - 1  OR sum -=1  (This is the same)
# E-Notation: 1,000,000 = 1 x 10**6 = 1e6